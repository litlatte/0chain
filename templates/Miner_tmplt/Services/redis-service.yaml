{{- $minerCount := .Values.miner.minerCount | int }}
{{- $minerCount := add $minerCount 1 | int }}
{{- range $i := untilStep 1 $minerCount 1 }}
---
apiVersion: v1
kind: Service
metadata:
  labels:
    {{- include "0helm.labels" $ | nindent 6 }}-redis-0{{ $i }}
  name: {{ $.Chart.Name }}-redis-0{{ $i }}
spec:
  selector:
    {{- include "0helm.selectorLabels" $ | nindent 6 }}-redis-0{{ $i }}
  ports:
  - protocol: {{ $.Values.miner.redisService.protocol }}
    port: {{ $.Values.miner.redisService.port }}
    targetPort: {{ $.Values.miner.redisService.targetPort }}

{{- end }}